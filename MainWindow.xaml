<Window x:Class="CyberSecurityChatbot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cybersecurity Chatbot" Height="450" Width="400">

    <Grid Margin="10">
        <StackPanel>

            <TextBlock Text="Cybersecurity Chatbot 🤖" 
                       FontSize="18" FontWeight="Bold" Margin="0,0,0,10" TextAlignment="Center" Width="380"/>

            <TextBox x:Name="ChatDisplay" 
                     Height="250" 
                     IsReadOnly="True" 
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"/>

            <TextBox x:Name="UserInput" 
                     Height="30" 
                     Margin="0,10,0,0"/>

            <Button Content="Send" 
                    Height="30" 
                    Click="Send_Click"/>
        </StackPanel>
    </Grid>
</Window>